@model IEnumerable<DagStukkie.Models.Gedagte>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GedagteTeks)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ChangeOperator)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GedagteTeks)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ChangeOperator)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GedagteID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GedagteID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GedagteID })
        </td>
    </tr>
}

</table>
